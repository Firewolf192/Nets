import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-types',
  imports: [RouterLink],
  templateUrl: './types.html',
  styleUrl: './types.css'
})
export class Types {

}
